import { provideHttpClient } from '@angular/common/http'; import { HttpTestingController, provideHttpClientTesting } from '@angular/common/http/testing'; import { TestBed } from '@angular/core/testing'; import { SceneMetricsService } from './scene-metrics.service';
describe('SceneMetricsService', () => { let service: SceneMetricsService; let http: HttpTestingController; beforeEach(() => { TestBed.configureTestingModule({ providers: [provideHttpClient(), provideHttpClientTesting()] }); service = TestBed.inject(SceneMetricsService); http = TestBed.inject(HttpTestingController); }); afterEach(() => http.verify()); it('maps metric stage timestamps and duration', () => { let value: any; service.get('scene').subscribe((result) => value = result); const request = http.expectOne('/api/scenes/scene/metrics'); request.flush({ scene_name: 'scene', updated_at: 'now', stages: [{ stage: 'train_3dgs', status: 'success', started_at: 'a', finished_at: 'b', duration_seconds: 12, metrics: {}, parameters: {}, artifacts: {}, source_path: 'metrics.json' }] }); expect(value.stages[0]).toMatchObject({ stage: 'train_3dgs', durationSeconds: 12, sourcePath: 'metrics.json' }); }); });
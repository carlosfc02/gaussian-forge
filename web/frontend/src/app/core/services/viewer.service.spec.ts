import { provideHttpClient } from '@angular/common/http'; import { HttpTestingController, provideHttpClientTesting } from '@angular/common/http/testing'; import { TestBed } from '@angular/core/testing'; import { ViewerService } from './viewer.service';
describe('ViewerService', () => { let service: ViewerService; let http: HttpTestingController; beforeEach(() => { TestBed.configureTestingModule({ providers: [provideHttpClient(), provideHttpClientTesting()] }); service = TestBed.inject(ViewerService); http = TestBed.inject(HttpTestingController); }); afterEach(() => http.verify()); it('launches the requested viewer', () => { let value: any; service.launchSugar('my scene').subscribe((result) => value = result); const request = http.expectOne('/api/scenes/my%20scene/viewers/sugar'); expect(request.request.method).toBe('POST'); request.flush({ scene_name: 'my scene', viewer: 'sugar', status: 'started', message: 'Opening', command: ['powershell'] }); expect(value).toMatchObject({ sceneName: 'my scene', viewer: 'sugar', message: 'Opening' }); }); });
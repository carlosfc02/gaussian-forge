import { PipelineLogDto, PipelinePresetDto, PipelineRunDto, PipelineStageDto } from '../../shared/dtos/pipeline.dto';
import { PipelineLog, PipelinePreset, PipelineRun, PipelineStage } from '../../shared/models/pipeline.model';
export function mapPipelineStageDto(dto: PipelineStageDto): PipelineStage { return { stage: dto.stage, status: dto.status, startedAt: dto.started_at, finishedAt: dto.finished_at, logPath: dto.log_path }; }
export function mapPipelineLogDto(dto: PipelineLogDto): PipelineLog { return { sceneName: dto.scene_name, runName: dto.run_name, stage: dto.stage, stages: dto.stages.map(mapPipelineStageDto), content: dto.content, truncated: dto.truncated, updatedAt: dto.updated_at }; }
export function mapPipelineRunDto(dto: PipelineRunDto): PipelineRun { return { sceneName: dto.scene_name, runName: dto.run_name, preset: dto.preset, status: dto.status, mode: dto.mode ?? 'full', stage: dto.stage ?? null, stages: dto.stages ?? null, options: dto.options ?? null, startedAt: dto.started_at, finishedAt: dto.finished_at, currentStage: dto.current_stage, manifestPath: dto.manifest_path, error: dto.error }; }
export function mapPipelinePresetDto(dto: PipelinePresetDto): PipelinePreset { return { name: dto.name, frameStep: dto.frame_step, sequentialOverlap: dto.sequential_overlap, iterations: dto.iterations, sugarMode: dto.sugar_mode, sugarRefinementTime: dto.sugar_refinement_time, runSugar: dto.run_sugar }; }